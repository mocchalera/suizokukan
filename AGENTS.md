# おえかきのうみ

- 原画の線、色、内部の白、塗り残しが主役。AIによる描き直し、採点、分類は禁止。
- 端末内の原写真と期限付き家族ルームコピーを分離する。明示確認した切り抜きPNG以外の画像を送信しない。
- 子どもにログイン、世話の義務、死亡、競争、自由チャットを要求しない。ひらがな中心、44px以上の操作、無音とreduced-motionで成立させる。
- React/TypeScriptと純Canvas。画素処理はWorker。画像・描画資源、カメラ、イベント、WebSocketを必ず解放する。
- API入力はsharedの検証を通し、秘密をURL query、フロントエンド、ログ、Gitに含めない。ルームcapabilityはfragmentで受け取り、sessionStorageへ退避しURLから除く。
- バックアップと同期データを信用しない。URL、HTML、巨大入力、異常PNG、非有限数を拒否し、永続化前に検証する。
- Jevは任意の性格文だけ。公式TypeSafe契約を使用し、未接続・timeoutは選択式設定へ戻す。別モデルへ代替しない。
- 公開・鍵・権限・プラン変更には明示許可が必要。この初版は指定repo main、指定Cloudflare Worker、衝突のないumi.mocchalera.appへの公開を許可済み。その他のプロジェクトは変更しない。
- 変更後はtypecheck、関連unit/e2eと実画面を検証。合成fixture、headless、実機、本番、Jev liveは区別して報告する。
- commitにはタスク所有のファイルのみ含め、force pushしない。
